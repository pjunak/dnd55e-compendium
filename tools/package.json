{
  "name": "dnd55e-compendium-tools",
  "private": true,
  "type": "commonjs",
  "description": "Dev-only migration tooling (NOT shipped/loaded by the addon). Reads a local Living-scroll checkout and emits the addon's data/*.js files.",
  "scripts": {
    "migrate": "node migrate.cjs"
  },
  "devDependencies": {
    "js-yaml": "^4.1.0"
  }
}
